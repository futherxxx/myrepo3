@model PCDetailsWeb.Models.tbllaptop

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>tbllaptop</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.id)

        <div class="form-group">
            @Html.LabelFor(model => model.fkbattery, "fkbattery", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("fkbattery", String.Empty)
                @Html.ValidationMessageFor(model => model.fkbattery)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.fksystem, "fksystem", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("fksystem", String.Empty)
                @Html.ValidationMessageFor(model => model.fksystem)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
